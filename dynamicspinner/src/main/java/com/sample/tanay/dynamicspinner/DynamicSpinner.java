package com.sample.tanay.dynamicspinner;

import android.app.Activity;
import android.content.Intent;

import java.util.ArrayList;

public final class DynamicSpinner {

    /**
     * start the service which will take care of all the data.
     * will throw an IllegalStateException if activity instance is null
     *
     * @param activity  instance to start a service
     * @param fileNames array list denoting the file names of the JSON descriptor file,
     *                  hierarchy file, and JSON data file
     */

    public static void setup(Activity activity, ArrayList<String> fileNames) {

    }
}
